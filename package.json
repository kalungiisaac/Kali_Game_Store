{
  "name": "game-store-library",
  "version": "1.0.0",
  "description": "A game store library application with RAWG and IGDB integration",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "cd server && node igdb-proxy.js",
    "start": "concurrently \"npm run server\" \"npm run dev\""
  },
  "devDependencies": {
    "vite": "^5.4.21"
  },
  "dependencies": {
    "concurrently": "^8.0.0"
  }
}
